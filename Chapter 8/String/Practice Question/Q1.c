#include <stdio.h>
int main(){
    // char firstName[50];
    // scanf("%s", &firstName);
    // printf("Your name is %s", firstName);

    char str[100];
    gets(str);
    puts(str);
    return 0;
}